ktor {
    deployment {
        port = 8080
    }
    application {
        modules = [ com.thematch.server.ApplicationKt.module ]
    }
}

jwt {
    secret = "test-jwt-secret"
    issuer = "thematch"
    audience = "thematch-users"
    realm = "The Match"
}
